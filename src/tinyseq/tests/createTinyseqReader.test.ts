import { createTinyseqReader } from '../createTinyseqReader';

describe( 'createTinyseqReader', () => {
  it.todo( 'test this' );

  // it( 'is a breadboard', () => {
  //   const read = createTinyseqReader( new Uint8Array( [
  //     12,
  //     0,
  //     12,
  //     128,
  //     12,
  //     127,
  //     12,
  //     128,
  //   ] ) );

  //   console.log( [ ...read() ].map( ( v ) => v.toFixed( 3 ) ).join( '\n' ) );
  //   console.log( [ ...read() ].map( ( v ) => v.toFixed( 3 ) ).join( '\n' ) );
  //   console.log( [ ...read() ].map( ( v ) => v.toFixed( 3 ) ).join( '\n' ) );
  //   console.log( [ ...read() ].map( ( v ) => v.toFixed( 3 ) ).join( '\n' ) );

  //   expect( true ).toBe( false );
  // } );
} );
2